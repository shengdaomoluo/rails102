<div class="col-md-12">
  <div classs="group">
    <%= link_to("修改",edit_group_path(@group), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @group.title %></h2>
  <p><%= @group.description %></p>
</div>
